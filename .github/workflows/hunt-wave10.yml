name: hunt-wave10
on:
  workflow_dispatch:
  push:
    paths:
      - "scripts/hunt_wave10.py"
      - ".github/workflows/hunt-wave10.yml"
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Run hunt wave10
        run: python3 scripts/hunt_wave10.py
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hunt-wave10
          path: |
            artifacts/
            out/
      - name: Summary
        if: always()
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path
          for p in [Path('artifacts/hunt_wave10.json'), Path('out/hunt_wave10.json')]:
            if p.exists():
              d = json.loads(p.read_text())
              print('confirmed', d.get('confirmed'))
              print('leads', d.get('leads'))
              print('webhook_hits', (d.get('probes') or {}).get('webhook_hits'))
              break
          PY
